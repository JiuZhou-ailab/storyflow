# Implementation Notes

## 2026-05-28

- Triaged the recent review findings for MVP usability. Deferred legacy model-gateway Worker hardening and banned-user edge cases because the active desktop path has moved to direct Cloudflare AI Gateway credential seeding; prioritized release correctness, chat rewind behavior, branchability persistence, and login gate state sync.
- Fixed official release packaging guardrails: the workflow now runs `check-version` before `validate:ci`, Windows release builds use the production `electron:dist:win` path instead of `electron:dist:dev:win`, and the macOS electron-builder retry helper now preserves the real failing exit code.
- Persisted `canBranch` metadata through `messageToStored` / `storedToMessage` so reloads do not resurrect unsafe branch buttons for provider messages that lacked native fork anchors.
- Preserved the source session `llmConnection` and `model` in rewind-created sessions. This keeps editing the first user message from silently switching to the workspace/global default model.
- Added a minimal client-auth state broadcast from main to every BrowserWindow, plus preload cache invalidation and renderer subscription, so a login or logout in one window updates the other restored/open windows without requiring reload.

- Removed `素材.md` from the short-form writing workspace's default structure. New short-form projects now scaffold only `创作要求.md`, `简报.md`, `大纲.md`, `人物.md`, `正文/`, and `自由区/`.
- Kept the removal scoped to the short-form common structure: Oh Story's `参考资料/`, generic external sources, chat attachments, and other method-pack material concepts are still valid separate surfaces.
- Treated legacy root `素材.md` as an ordinary unknown file for the writing catalog. It is no longer a short-form detection anchor, sidebar global-info item, required path, skill prompt dependency, or workspace creation preview asset.

- Simplified the desktop auth/model boundary for stability. Login now only gates app access; Neon email login no longer calls the auth broker for a model gateway token, and Feishu login ignores any legacy `gatewayToken` returned by the broker.
- Switched bundled managed model URLs from the Storyflow model-gateway Worker to direct Cloudflare AI Gateway custom provider URLs. Per the latest scope, `wangsu-default` is the only bundled managed connection; the Xiaomi fallback experiment is not included in active defaults.
- Updated packaged build validation and release env wiring so a direct model gateway token is required when desktop auth is enabled. Managed model credential failures now point to model settings/support instead of reauth, because they are no longer caused by login session state.
- The earlier Worker/JWT path failed after restart because local desktop credentials could drift from what the Worker expected. The current investigation is constrained to Wangsu; if direct Wangsu still returns provider-side `auth_failed`, the fix belongs in the Cloudflare custom provider credential/upstream route rather than adding Xiaomi.
- Verified Wangsu direct Cloudflare AI Gateway smoke tests with `cf-aig-authorization` only: `gpt-5.5`, `gemini-3.5-flash`, and `deepseek-v4-pro` all returned HTTP 200. Sending both `Authorization` and `cf-aig-authorization` can reproduce upstream `auth_failed`, so the managed connection must stay on Pi's `cloudflare-ai-gateway` provider where `authHeader: false` avoids the ordinary bearer header.
- Investigating CLI `--validate-server` E2E failures after the named Bun E2E tests passed. The first failure was environmental: an existing Electron server owned the default lock, so validation is run with an isolated `CRAFT_CONFIG_DIR`.
- Added the missing repository automation validation template at `.github/agents/automations.json`. The template uses deterministic matcher IDs so prompt automation history can be attributed on the first reload, not only after a later ID backfill.
- Changed CLI validation to write `automations.json` through the server `file:write` RPC instead of direct filesystem writes. The server now notifies the workspace ConfigWatcher for `automations.json` writes so the in-memory AutomationSystem reloads before status-change validation.
- Changed the webhook validation RPC payload to include a deterministic `automationId` and use `POST`, matching the follow-up history assertion that looks for a recent failed POST webhook entry.
- Superseded the broker-signed model gateway JWT experiment. `apps/auth-broker-worker` and the local Web UI auth broker now return login identity/app-session data only; they no longer issue `gatewayToken` for desktop model access.
- Kept the model-gateway Worker code as a legacy artifact for now, but it is no longer on the desktop default path. The current path is direct Cloudflare AI Gateway seeded from `CRAFT_CLIENT_GATEWAY_TOKEN`.
- Added and deployed `apps/auth-broker-worker` as the packaged desktop HTTPS auth broker at `https://storyflow-auth-broker.d1095245867.workers.dev`. In the simplified path it exposes Feishu public config, Feishu code exchange, and Neon token exchange without shipping Feishu secrets to Electron.
- Kept the macOS dotenv loader non-evaluating so explicit CI env cannot be clobbered by local `.env`, but added a narrow `cat`-file resolver for existing local signing values like `CSC_KEY_PASSWORD="$(cat /path/to/password)"`. Bun may pre-load that value as `(cat /path/to/password)`, so the script supports both forms without restoring arbitrary shell evaluation.
- Added temporary p12 normalization for local macOS signing: old RC2/3DES p12 files are read with OpenSSL legacy support and re-exported with `/usr/bin/openssl` into the build temp directory so macOS Keychain can import them. No normalized private key material is persisted beyond the build temp directory.
- Local release verification for `0.9.19` produced signed/notarized arm64 artifacts at `apps/electron/release/Storyflow-arm64.dmg` and `apps/electron/release/Storyflow-arm64.zip`. Manual checks passed for `codesign --verify --deep --strict`, `spctl --assess`, and `xcrun stapler validate` on both app/DMG where applicable.
- Fixed an OpenAI-compatible SSE edge case from the managed Wangsu route: after tool-call deltas, the upstream can send `[DONE]` without a `finish_reason`. The interceptor now emits a synthetic `finish_reason: tool_calls` after flushing consolidated tool calls so Pi SDK does not surface `Stream ended without finish_reason`.
- Preserved user-selected local default LLM connections when applying bundled managed defaults. The hidden managed connections are still inserted/updated, but they no longer overwrite an existing valid custom default; this keeps local API Key configuration parallel to login-managed access.
- Changed the managed Wangsu connection's per-connection default model from `gemini-3.5-flash` to `gpt-5.5` in both the current `builtinLlmConnections` entry and legacy `builtinLlmConnection` fallback. The model list and Cloudflare AI Gateway/OpenAI-compatible route stay unchanged.
## 2026-05-28

- Optimizing Electron package size by changing package inputs rather than removing runtime capabilities. Electron, the Claude native SDK binary, Bun, and uv remain intentionally bundled; the first pass targets duplicated resources, dev files, and source maps.
- Chose `app/dist/resources` as the single packaged runtime resource root because bundled docs/themes/config already resolve relative to the compiled main-process `__dirname`. `app/resources` remains a legacy backend fallback for older artifacts, but new packaged CLI env vars point at `app/dist/resources`.
- Added package-size regression tests before implementation. The tests require explicit platform file allowlists, reject duplicate top-level `resources/**` package patterns, prefer `dist/resources` for packaged subprocess server resolution, and make renderer source maps opt-in.
- Kept implementation serial after read-only parallel exploration because `electron-builder.yml`, runtime resource paths, and build scripts share one package contract. Parallel writes here would risk conflicting ownership of the same release surface.
- Verified the package-selection change with `CSC_IDENTITY_AUTO_DISCOVERY=false npx electron-builder --mac --arm64 --dir -c.mac.forceCodeSigning=false -c.mac.notarize=false`. The generated unpacked app keeps only `app/dist/resources` for staged runtime assets; `app/resources`, app `packages`, app source/config files, and `*.map` are absent. The local unpacked `.app` is now 684M versus the prior 790M snapshot, before producing a signed/notarized DMG.

- Collapsed the hidden managed Cloudflare gateway back to a single `wangsu-default` bundled default. `builtinLlmConnection` remains as a legacy schema fallback and points to the same Wangsu route.
- Corrected the bundled hidden managed route to the direct Cloudflare AI Gateway custom Wangsu provider endpoint. `CRAFT_CLIENT_GATEWAY_TOKEN` is a direct Cloudflare AI Gateway token seeded locally; desktop no longer stores broker JWTs for model access.
- Kept generic custom endpoints on the synthetic `custom-endpoint` provider, but registered Cloudflare-managed custom models under Pi's `cloudflare-ai-gateway` provider. This preserves Pi SDK's built-in `cf-aig-authorization` request header instead of sending the managed token as a normal OpenAI `Authorization` key.
- Normalized managed default gateway auth failures at the session boundary, not in the provider adapter. Pi `message_end` errors can arrive as `typed_error` after SDK event adaptation; for managed defaults those are now presented as model-service/settings problems instead of login reauth.
- Skipped automatic token-refresh retry for managed default auth failures. The hidden gateway credential is locally seeded, so retrying the same turn cannot repair a bad Cloudflare/upstream credential.
- Split desktop login identity from model gateway credentials. Email/Neon and Feishu login no longer write model credentials; model credentials come from bundled/local Cloudflare AI Gateway configuration.
- Cleaned the local `~/.craft-agent` provider state after backing it up. The active managed default should now be `wangsu-default`; stale broker/JWT-style LLM credentials should be removed so they cannot keep producing gateway 401s.
- Replaced the earlier interim Workers AI managed route with the Cloudflare AI Gateway `custom-wangsu` route above. Bundled defaults, onboarding presets, and managed metadata sync now converge on `cloudflare-ai-gateway` for `wangsu-default`.
- Verified the Wangsu upstream accepts OpenAI-compatible Chat Completions payloads but rejects OpenAI Responses payloads with `field messages is required`, so the managed default stays on `openai-completions`. Cloudflare's `/openai/responses` and `/ai/v1/responses` support is for Cloudflare's OpenAI/native REST paths, not proof that this custom Wangsu provider accepts Responses-shaped `input`.
- The earlier Feishu-login-managed-model path assumed the Wangsu provider key was stored behind Cloudflare AI Gateway / Provider Keys. This is now superseded by local direct Cloudflare credential seeding.
- Fixed the local auth broker startup contract for desktop login. Email/Neon Auth can now start the broker without Feishu secrets, and the dev broker no longer participates in model credential exchange.
- Archived the Xiaomi MiMo investigation; it is not part of the active bundled managed route.
- Confirmed the provided Cloudflare `cfat-` token now has enough AI Gateway permission to update custom providers, but Provider Config / BYOK creation still returns `403 Authentication error`. The token still needs Secrets Store write scope before it can store the MiMo key in Cloudflare BYOK.
- Updated the Cloudflare custom providers to domain-root `base_url` values after live requests showed pathful `base_url` values caused incorrect upstream path composition. The desktop managed Wangsu gateway base URL now carries the fixed Wangsu prefix after `custom-wangsu`, matching the Cloudflare route that returned HTTP 200.
- Removed Xiaomi from the active desktop defaults and onboarding preset. Any future Xiaomi route should be introduced only as a deliberate product decision, not as an automatic fallback for Wangsu.
- Created a dedicated Feishu Open Platform internal app named `Storyflow` because the previous local `client_id` belonged to a personal app displayed as `飞书 CLI`. The new app uses `http://localhost:6477/callback` as the desktop OAuth redirect URL, and only the local broker/server `.env` stores the app secret.
- Collapsing the manuscript/free-area title actions into a single compact dropdown trigger instead of adding a new toolbar or widening the sidebar row. The hidden actions keep the existing import/create handlers and order, so the interaction changes from two always-visible icons to one click-revealed menu without changing file semantics.
- Moved queued mid-stream user messages out of the transcript UI and into a compact preview above the composer. The underlying queued message stays in session state for backend replay, but ChatDisplay now filters it out of turn grouping/search/current-turn timing so the previous response remains visually active until it finishes.

## 2026-05-27

- Investigating a writing editor selection bubble drift where the formatting and AI prompt BubbleMenus are appended under TipTap's editable DOM inside the scrollable document content. The intended fix is to anchor floating UI to the stable editor shell, listen to the editor content scroll target for repositioning, and hide when the selected reference leaves the clipping boundary.
- Implemented the selection bubble fix at the shared `TiptapMarkdownEditor` / `TiptapBubbleMenus` boundary. The two text-selection menus now use the editor shell as `appendTo`, the `.tiptap-editor-content` element as Floating UI `scrollTarget`, and `hide: true`; rich block and inline math menus are left unchanged for now because the reported drift is selection-text specific and those menus have custom node anchors.
- Revised the selection bubble behavior after live feedback: using Floating UI `scrollTarget` alone still lets TipTap chase the selection during scroll and snap back after debounce. Text selection menus are now dismissed immediately on editor-content scroll and only re-enabled by a subsequent selection update, preventing stale menus from appearing over unrelated manuscript text.
- Reworked the Web UI login page as a conventional responsive auth shell instead of a single centered card. Desktop now separates brand context from the auth form; mobile collapses to one column. The auth logic stays on the existing Neon/email, Feishu, and server-token endpoints.
- Kept server-token login as a fallback method and made dividers conditional. This avoids showing a bare `or` when only one auth method is configured, while preserving deployments that still rely on `CRAFT_SERVER_TOKEN`.
- Revised the Web UI login visual direction back toward the main app chrome: a compact single auth card, system font, app-like oklch foreground/background tokens, purple accent, minimal border/shadow, and no marketing-style brand panel.
- Audited skill loading for global/local behavior before adding writing skills. Craft loads global skills from `~/.agents/skills`, workspace skills from `{workspace}/skills`, and project skills from `{workingDirectory}/.agents/skills` with project > workspace > global precedence.
- Treated the screenshot requirements as global Craft skills, not short-form Method Pack-local skills. Existing global skills cover outline generation, ideation, short-form diagnosis, plot causality, timeline, character, chapter unit, and state ledger; missing global skills are being added under `~/.agents/skills`.
- Consolidated the screenshot coverage to avoid one-function-per-skill duplication: `prose-drafting` covers continuation and outline-to-prose, `prose-revision` covers expansion/polish/de-AI, `story-deconstruction` covers deconstruction and benchmark rewrite, and AI correction is folded into `plot-causality-audit`.
- Chinese-localized all global Craft writing skill metadata and UI prompts under `~/.agents/skills`. Folder slugs remain English as stable IDs, while visible names, descriptions, bodies, and default prompts are Chinese.
- Rechecked duplicate boundaries: overlaps are intentional trigger overlap, not duplicate ownership. Drafting, revision, deconstruction, ideation, outlining, causality, timeline, character, chapter-unit, state-ledger, and short-form diagnosis remain separate responsibilities.
- Noted a live-refresh boundary: global skills are watched directly, but project-level `.agents/skills` outside the workspace are only picked up when `workingDirectory` is passed through explicit reload paths; some watcher callbacks reload without projectRoot.
- Localized the generated short-form Method Pack skills at the source (`short-opening-designer`, `short-golden-three`, `short-draft-chapter`, `short-reviser`) while keeping their English slugs as stable IDs. New short-form workspaces now generate Chinese visible skill names, descriptions, headings, and bodies.
- Migrated existing generated short-form workspace skill copies under `~/.craft-agent/workspaces` to the Chinese templates. Legacy short-article skills were translated in place when they still had the old generated marker, but were not deleted because they may belong to older user workspaces.
- Fixed the post-login project layout path at two separate layers: new Electron windows now reset renderer zoom to `1`, and the shell no longer resolves desktop sidebar/workspace proportions while the container is still in compact/narrow measurement. The writing workspace width is also clamped so the assistant panel keeps its minimum usable width.
- Historical note: a short Xiaomi fallback was considered, then removed after the scope was narrowed back to Wangsu-only.
- Kept SDK-fork branch safety intact. Missing Pi/Claude provider anchors still reject on the server; the UI now receives/stores `canBranch` metadata and rewind selection skips assistant messages that cannot be safely forked, avoiding the internal "Pi branch anchor missing" error path for old/incomplete turns.
- Added a broad session-turn watchdog instead of per-tool/provider timeout plumbing. The policy is intentionally generous but bounded: 30 minutes without any agent event is treated as idle timeout, and 12 hours is the absolute hard turn limit.
- Kept timeout policy isolated in `packages/server-core/src/sessions/turn-watchdog.ts` so `SessionManager` only starts the watchdog, marks progress on agent events, and performs one existing `AbortReason.Timeout` cleanup path.
- Timeout cleanup adds a visible error message, calls provider `forceAbort(AbortReason.Timeout)`, and reuses `onProcessingStopped(..., 'timeout')` so queued messages and browser cleanup follow the existing session lifecycle.
- This first pass deliberately does not add separate MCP/tool/permission TTLs. Those would be more precise but would spread timeout policy across many adapters; the current fix is a coarse recovery rail with minimal code pollution.
- Added a Neon-side read view `public.craft_auth_users` on project `default` / database `neondb` so Auth users are visible from the default `public` schema without selecting sensitive Better Auth account fields. The source of truth remains `neon_auth."user"`; the view currently exposes Neon Auth email/password users only, not Feishu OAuth logins handled outside Neon Auth.
- Historical note: `wangsu-default` remains the hidden default connection whenever bundled defaults are applied.
- Tightened the default gateway credential contract: remote custom endpoints are no longer considered keyless just because they have a base URL, and managed default auth failures are surfaced as model-service/settings problems instead of asking end users to update a normal API key.
- Moved the WindowManager native-window regression test to `window-manager.isolated.ts`. It mocks Electron at module scope, and Bun keeps module mocks process-wide within a test run; isolating it prevents the partial `electron`/`logger` mocks from leaking into BrowserCDP, deep-link, and browser-pane tests.
- Softened long writing-catalog file titles in the shell sidebar by letting compact file items use a two-line clamp with a stable minimum row height. This keeps full paths in the existing `title` tooltip, avoids horizontal scrolling in the navigator, and does not add path-specific parsing rules for Chinese chapter filenames.
- Split desktop Feishu OAuth into a public-client flow plus server-side broker exchange. Packaged Electron can now build the authorize URL with `CRAFT_CLIENT_FEISHU_APP_ID`, receive the loopback callback, and send `code` plus the PKCE verifier to `CRAFT_CLIENT_AUTH_BROKER_URL`; Feishu app secrets stay on the broker.
- Added `/api/client-auth/feishu/exchange` as the minimal broker contract on the Web UI/server handler. It exchanges the code server-side, reuses the existing internal-tenant/registration policy, returns a public Feishu user plus an app session token, and rejects non-loopback redirect URIs.
- Simplified the desktop auth broker client without changing the broker protocol. Neon and Feishu exchanges now share one request/JSON/error path, and Feishu configuration is checked before opening the local callback server.
- Simplified the Web UI Neon exchange boundary by sharing token extraction, rate limiting, verification, and rejection logging between browser login and desktop client-auth broker exchange. The two routes still mint different session artifacts.
- Moved packaged desktop auth build validation into a pure helper and widened the guard from Feishu-only to all required desktop client auth. Packaged builds need a deployed HTTPS auth broker unless `CRAFT_DEV_RUNTIME=1`; model access is now validated separately through the direct Cloudflare gateway token.
- Fixed downloaded desktop Feishu login using a stale packaged app ID. The broker now exposes `/api/client-auth/feishu/config` with only public OAuth fields, and the desktop client fetches that config before opening Feishu so the consent page shows the app configured on the current auth broker deployment. Packaged `CRAFT_CLIENT_FEISHU_APP_ID` remains only a fallback for older brokers.
- Updated the Electron main build defines to include only public desktop auth bootstrap env vars. Feishu app secrets and model gateway keys are intentionally excluded from build-time defines and from the example distributed client config.
- Superseded the earlier client-auth-to-model-credential wiring. Successful client auth now only establishes app identity; managed model credentials are seeded from local/bundled model configuration and are not cleared on sign-out.
- Removed the Electron-side Feishu app-secret fallback. Distributed desktop builds now treat Feishu as a public OAuth client: `CRAFT_CLIENT_FEISHU_APP_ID` plus `CRAFT_CLIENT_AUTH_BROKER_URL` are allowed in the client, while `CRAFT_WEBUI_FEISHU_APP_SECRET` stays only on the broker/server.
- Recovered local macOS Developer ID signing without creating a new usable certificate. The downloaded Developer ID Application certificate matches the existing private key under `~/.codex/secrets/storyflow-macos-release`, not the newer CSR/key pair under `~/.storyflow/apple-signing`; the matching cert/key pair was imported into the login keychain and now appears as a valid codesigning identity.
- Added local `.env` signing pointers for `APPLE_SIGNING_IDENTITY`, `CSC_LINK`, and `CSC_KEY_PASSWORD` using the existing `storyflow-macos-release` p12 material. `CRAFT_REQUIRE_MAC_SIGNING` remains unset because notarization credentials are still separate from the Developer ID certificate.
- Verified the signing identity by signing a temporary executable. The signature verifies locally; Gatekeeper rejects it only as `Unnotarized Developer ID`, which confirms signing works and the remaining distribution blocker is Apple notarization credentials.
- Corrected macOS notarization credential handling for App Store Connect API keys. `APPLE_API_ISSUER` is now optional and only used for Team API keys; Individual API keys must provide `APPLE_API_KEY_BASE64`/`APPLE_API_KEY` plus `APPLE_API_KEY_ID` without an issuer.
- Confirmed the existing App Store Connect Team API key `69KNT3WRS7` belongs to Issuer ID `4876acbd-c986-4873-92b4-b22e49ebd596` and passes `notarytool history` when the issuer is supplied. Local `.env` now points `APPLE_API_KEY`, `APPLE_API_KEY_ID`, and `APPLE_API_ISSUER` at that verified notarization credential set.
- Found that a successful app-bundle notarization is not enough for downloaded distribution: the generated DMG was initially rejected by Gatekeeper because it had no usable DMG signature or stapled ticket. The release config now signs the DMG through electron-builder and the build script submits/staples/verifies the DMG separately after packaging.
- Optimized public installer downloads without changing updater contracts. GitHub Release assets and update manifests keep stable names like `Storyflow-arm64.dmg`, while the R2 publisher now also creates versioned installer aliases like `Storyflow-<version>-arm64.dmg`; the marketing download buttons point at those aliases and include a matching `download` filename.

## 2026-05-26

- Implementing Feishu login at the Web UI auth boundary, not in the Lark messaging bot path: the project already has a browser `/login` flow, while the bot adapter only authenticates platform traffic.
- Keeping password login as a fallback so existing headless deployments using `CRAFT_SERVER_TOKEN` / `CRAFT_WEBUI_PASSWORD` keep working.
- Treating the company-internal rule as a Feishu `tenant_key` allow-list. External Feishu accounts must match an active registration row in the configured Postgres/Neon store.
- Neon MCP did not complete its handshake in this session, so no live Neon project or credential was created. Based on official Neon regions, China-side latency should prefer `aws-ap-southeast-1` (Singapore); the chosen project region cannot be changed after project creation.
- Avoiding a hard dependency on a specific Neon project in source code. Runtime config will use `CRAFT_WEBUI_AUTH_DATABASE_URL`, with schema creation handled lazily by the registration store.
- Added unauthenticated Web UI endpoints under `/api/auth/feishu/*`: `config`, `start`, and `callback`. OAuth `state` plus PKCE protects the browser callback before a Craft session cookie exists.
- Added `CRAFT_WEBUI_FEISHU_APP_ID`, `CRAFT_WEBUI_FEISHU_APP_SECRET`, `CRAFT_WEBUI_FEISHU_REDIRECT_URI`, `CRAFT_WEBUI_FEISHU_SCOPE`, `CRAFT_WEBUI_FEISHU_INTERNAL_TENANT_KEYS`, and `CRAFT_WEBUI_AUTH_DATABASE_URL`. Feishu login is enabled only when app ID and secret are both present.
- The Postgres registration table is `webui_feishu_users`. Manual external registration should set `status = 'active'` on a matching `open_id`, `union_id`, `user_id`, `email`, or `enterprise_email`; unregistered external login attempts are recorded as `pending` when the database URL is configured.
- Retested Neon MCP after OAuth login. The available `default` project is in `aws-ap-southeast-1` and runs PostgreSQL 17.10. A temporary branch `codex-feishu-auth-test-20260526` was created for validation, the `webui_feishu_users` DDL and registration lookup behavior were verified there, and the temporary branch was deleted afterward.
- Starting a minimal Neon Auth bridge for email sign-up/sign-in. The auth boundary stays at Web UI session issuance: Neon Auth verifies user identity, then the server issues the existing `craft_session` cookie so model access still goes through the current gateway.
- Deferring token grants, quotas, and billing tables. The first stable target is one unified user-facing auth surface; accounting can be added later behind the same session subject without changing the login contract.
- Avoided adding a browser SDK dependency after Bun workspace install attempted to resolve the local WebUI package from npm. The implemented path uses server-side Neon Auth REST calls for email sign-in/sign-up, then verifies the returned JWT with JWKS before issuing the local session cookie.
- Provisioned Neon Auth on the Neon `default` project main branch (`young-cherry-15654428` / `br-square-feather-a1kv8pnz`) for database `neondb`. Email/password sign-up and sign-in are enabled without mandatory email verification for the first stable auth pass; local trusted origins are limited to localhost/127.0.0.1 ports 9100 and 3100.
- Verified the branch-specific Neon Auth OpenAPI schema and corrected the server bridge to call `/sign-in/email` and `/sign-up/email` relative to the Neon Auth base URL, not `/auth/sign-in/email`.
- Added username login as a server-side mapping to Neon Auth email/password: `zjding` becomes `zjding@users.craft.invalid` when `CRAFT_WEBUI_NEON_AUTH_USERNAME_EMAIL_DOMAIN=users.craft.invalid` is set. This keeps Neon Auth as the only identity store while preserving a short username login for the owner account.
- Added `CRAFT_WEBUI_PASSWORD_AUTH_ENABLED=false` so deployments can require account auth and hide/reject the old server-token Web UI login without changing the RPC server token contract.
- Neon Auth `/sign-in/email` returns a Better Auth opaque session token in the top-level `token` field. The Web UI bridge must treat that as a session token, read the auth cookie, call `/token`, and verify the returned JWT before issuing `craft_session`.
- Clarified that the earlier Neon Auth Web UI bridge only protects the browser/HTTP Web UI path. `bun run electron:dev` starts the Electron renderer directly and does not visit that Web UI login page.
- Added Electron client auth as a separate startup gate: the main process owns Neon Auth sign-in state, preload exposes only auth IPC for login, and the renderer lazy-loads `App` only after the account is authenticated.
- Reused the existing Neon Auth config for the Electron client to avoid a second identity store. The local `.env` now sets `CRAFT_CLIENT_AUTH_REQUIRED=true`; client-specific Neon env vars can override the Web UI Neon values later if needed.
- Kept Electron auth state process-local for this first stable pass. There is no persisted refresh token and no billing/quota layer yet; restarting the app requires signing in again.
- Preload now blocks normal RPC invoke calls before client auth succeeds. This is a client startup/user-flow gate for the desktop app, not a hard security sandbox against someone modifying local app code.
- Fixed Electron email/password login by always passing a trusted Origin to Neon Auth. The default is `http://localhost:9100`, configurable with `CRAFT_CLIENT_NEON_AUTH_ORIGIN`; the value must stay in Neon Auth trusted origins.
- Added Electron Feishu OAuth login to the same client auth gate. The main process opens Feishu in the system browser, listens for `http://localhost:6477/callback`, and sends the code plus PKCE verifier to the configured auth broker.
- Electron Feishu client config is intentionally public-client-only. Access policy and Feishu app secret live on the broker/server; for Electron, register `http://localhost:6477/callback` in the Feishu Open Platform app.
- Electron Feishu login no longer reads client-side allow-all or tenant allow-list env vars. Those decisions belong to the broker so the distributed app cannot be changed into a different access policy by local env edits.
- Added a Feishu login timeout and cancel path for Electron. If Feishu rejects or never calls back to `http://localhost:6477/callback`, the auth gate no longer spins forever and the loopback callback server is closed before retry.

## 2026-05-24

- Added a visible `session.newSession` label to the chat header new-session action while keeping the existing tooltip, aria label, context menu trigger, and meta/ctrl-click new-panel behavior unchanged.
- Kept the change at the ChatPage call site instead of widening `PanelHeaderCenterButton`, because this is a single product-labeling request and the shared button still works as an icon-first primitive for other header controls.
- Classified `Provider finish_reason: content_filtered` as a structured provider-safety error instead of letting the raw provider stop reason become a plain session error. This keeps the original error available for diagnostics while giving the UI an actionable typed error.
- Removed the top-level `analysis/` directory from new Claude-Book scaffolds. Analysis artifacts now live under `.work/analysis/`, matching the existing contract that `.work/` owns temporary drafts, reports, and scratch analysis. Existing `analysis/` folders are still recognized for legacy projects.
- Hid the writing workspace Analysis tab/section when no analysis files exist, so new workspaces do not show an empty fixed analysis bucket.
- Flattened short-form workspace `全局信息` in the shell sidebar so `创作要求.md`、`简报.md`、`大纲.md`、`人物.md`、`素材.md` appear as direct items in Method Pack order instead of nested category folders.
- Kept the internal `analysis` classifier for legacy and non-short-form layouts, but short-form workspaces no longer expose an Analysis tab when `素材.md` exists.
- Investigated the packaged-client page-switch flicker as a native paint issue rather than a router reload: renderer navigation uses `history.pushState`, and the packaged renderer files match the local built renderer in the existing release output.
- The main Electron window had no native `backgroundColor`, while the browser-pane window already sets one explicitly to prevent black/white paint flashes. I reused the same light/dark background values for the main window.
- Kept the fix at the BrowserWindow boundary instead of making the renderer root opaque, because the app intentionally uses transparent/vibrancy-backed surfaces on macOS.

## 2026-05-22

- Confirmed `electron:dev` failure is consistent with a wedged local esbuild binary/service: `node_modules/@esbuild/darwin-arm64/bin/esbuild --version` did not return within 3 seconds.
- Treating the dev-start failure as an environment dependency repair first, not as a source regression in `packages/session-mcp-server`.
- Keeping edits scoped to chat layout, diff extraction/rendering, tests, and this notes file. The repository already contains unrelated modified files that are intentionally left untouched.
- Refreshed dependencies with `bun install --force`; after that, the esbuild binary returned `0.25.12` immediately and the session MCP entry reached normal CLI argument validation.
- Added a Write diff guard: captured previous-content metadata that exactly matches the new content is treated as absent, so a new-file write renders as additions instead of identical red/green content.
- Tightened chat transcript scroll containment by giving the shared ScrollArea an explicit viewport class hook and applying `min-h-0 overflow-y-auto` to the transcript viewport.
- During release verification, full tests exposed a stale short-form starter message contract. I expanded the user-facing starter copy to match the localized four-section onboarding expected by workspace creation tests.
- Full-suite source tests exposed a leaking Bun module mock in token refresh tests. I replaced the storage-module mock with an injected persistence hook on `TokenRefreshManager`, so the tests no longer contaminate global source storage imports.
- Release validation showed the old public R2 hostname resolves through an unusable local/proxy address for installer downloads. I moved the default release/update base URL to `story-storage.zjding.com`, updated the GitHub Actions variable, and will cut a new patch release instead of mutating the already-built `v0.9.15` artifacts.
