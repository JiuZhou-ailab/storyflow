diff --git a/dist/api/google-shared.js b/dist/api/google-shared.js
index 4602d2c41451118779de1946f9890251be1a3d0b..23914f41ab679fa6f6b0b0f8f4a97d15e88296ef 100644
--- a/dist/api/google-shared.js
+++ b/dist/api/google-shared.js
@@ -159,7 +159,11 @@ export function convertMessages(model, context) {
                     }
                 }
                 else if (block.type === "toolCall") {
-                    const thoughtSignature = resolveThoughtSignature(isSameProviderAndModel, block.thoughtSignature);
+                    // Gemini 3 rejects replayed function calls without a thought signature.
+                    // Cross-provider history cannot have a genuine Gemini signature, so use
+                    // Google's documented sentinel instead of flattening structured tool context.
+                    const thoughtSignature = resolveThoughtSignature(isSameProviderAndModel, block.thoughtSignature) ??
+                        ((getGeminiMajorVersion(model.id) ?? 0) >= 3 ? "skip_thought_signature_validator" : undefined);
                     const part = {
                         functionCall: {
                             name: block.name,
