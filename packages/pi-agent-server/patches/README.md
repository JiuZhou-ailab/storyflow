# Pi Dependency Patches

Version-pinned Bun patches for Pi protocol defects; delete each patch as soon as the corresponding fix ships in `@earendil-works`.

- `@earendil-works%2Fpi-ai@0.84.1.patch` — preserves Gemini 3 cross-provider tool history with Google's documented unsigned-call sentinel.
