// input: User-authored global instruction text
// output: Browser-safe limits shared by the editor and persistence boundary
// pos: Public contract for Storyflow's user-level system instruction module

export const MAX_SYSTEM_INSTRUCTIONS_CHARS = 20 * 1024;
