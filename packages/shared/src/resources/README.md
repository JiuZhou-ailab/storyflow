# Resources

`resolver.ts` owns global/project resource scope; `resource-bundle.ts` owns portable import/export; `install-receipts.ts` owns local Market provenance and explicit Skill upgrades; `types.ts` and `index.ts` expose their contracts.
