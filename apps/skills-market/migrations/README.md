# D1 migrations

`0001_initial.sql` creates publisher identity, Skill metadata, and immutable versions; `0002_ai_review.sql` adds admission evidence; `0003_visibility.sql` separates public and company-scoped discovery; `0004_download_metrics.sql` adds atomic popularity counters shared by curated and member-published Skills. Package bytes remain in private R2.
