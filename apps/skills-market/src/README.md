# Worker source

`catalog.ts` curates source-linked Skill recommendations; `packages.ts` validates real published bundles; `review.ts` owns automated admission; `index.ts` owns the API/bearer/D1/R2 boundary; `local-dev.ts` adapts the API to Bun; `*.test.ts` lock the contract.
