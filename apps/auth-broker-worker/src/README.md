# Auth Broker Worker Source

Worker request handling and contract tests for verified identity exchange, optional Neon Organization admission, and short-lived model/Market JWT issuance.
Files: `index.ts`, `index.test.ts`.
