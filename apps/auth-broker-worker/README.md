# Auth Broker Worker

HTTPS desktop auth broker for Feishu/verified-Neon identity exchange, optional Organization admission, renewable model access, and ephemeral Skills Market publication.
Files: `wrangler.toml`, `package.json`, and `src/` for signed-claim verification, scoped token issuance, key rotation, and tests.
