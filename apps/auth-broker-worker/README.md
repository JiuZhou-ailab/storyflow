# Auth Broker Worker

HTTPS desktop auth broker for Storyflow login, renewable model access, and ephemeral Skills Market publication.
Files: `wrangler.toml`, `package.json`, and `src/` for Feishu/Neon exchange, scoped token issuance, key rotation, and tests.
