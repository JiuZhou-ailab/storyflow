# Storyflow Catalog MCP

Streamable HTTP MCP adapter exposing fixed read-only Catalog operations, including bounded video-asset search; VPN-private deployments rely on the network boundary, while public relays require `MCP_BEARER_TOKEN`. It never receives database credentials or arbitrary SQL. Files: `src/`, `package.json`, `bun.lock`, `Dockerfile`.
