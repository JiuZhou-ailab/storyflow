# Account components

Account and local usage sections that read existing root-owned auth and persisted session facts without creating separate routes or storage.
Files: `AccountSettingsSection.tsx`, `LocalUsageSection.tsx`, `local-usage.ts`, `index.ts`, and focused tests under `__tests__/`.
