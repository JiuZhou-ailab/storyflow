# Electron Build Resources

Platform-specific files consumed directly by Electron Builder during packaging.

- `entitlements.mac.plist` — macOS hardened-runtime entitlements.
- `installer.nsh` — Windows NSIS hook that provisions Git for Windows on fresh installs.
