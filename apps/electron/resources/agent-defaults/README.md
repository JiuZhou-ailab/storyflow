# Agent Defaults

`global-skills/` 保存产品随包资源，启动时只补齐 `find-skills`、`skill-creator`、`anysearch`、`storyflow-tutorial` 与 `sn2s-novel-to-screenplay`；`sources/` 保存首次启动时补齐的全局 Sources，包括 typed `storyflow-catalog` 与 `wangwen-bigdata`。其他 Skills 由用户创建或从 Storyflow Skills Market 显式安装，不随应用默认加载。
