# 0.9.25

## 更新检查

- 修复 macOS 自动更新对旧下载缓存的误判。客户端现在会按本次更新 manifest 校验下载文件与 `sha512`，避免把旧版本缓存当作新版本已下载完成。
- 在开发或未打包环境点击设置里的更新检查时，会明确提示只能在正式安装包中检查更新，避免误判为线上没有新版本。
- 发布流程新增手动 `electron-hotfix` 验证 profile，桌面端更新链路的紧急修复可以先跑聚焦测试、Electron 类型检查和相关 ESLint，再进入签名、打包和 R2 发布。
