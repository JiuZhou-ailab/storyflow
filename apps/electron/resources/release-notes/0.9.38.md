# 最新动态

这一版修复了 macOS 自动更新没有真正安装的问题。点击「更新并重启」后，Storyflow 会先保存当前状态，再把退出和应用替换完整交给系统更新器。

## 自动更新

- **重新打开就是新版本**：修复了更新包已经下载、应用却只退出而没有完成替换的问题，安装完成后会自动打开新版本。
- **安全保存后再安装**：更新前会先完成会话、窗口状态和后台资源的清理，避免安装流程与应用退出互相打断。
- **项目加载性能保持不变**：修复只调整退出与安装的交接，不会让对话历史或后台初始化重新阻塞项目打开。
